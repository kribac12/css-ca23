/*export function getToken() {
  const token = localStorage.getItem("jwtToken");

  if (!token) {
    console.error("No token retrieved, the user must log in.");
    window.location.href = "/index.html";
  }
  return token;
}
*/
